<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.communityeats">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommunityEats"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activities.UpdateProfileActivity"
            android:exported="true" />
        <activity

            android:name=".activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--        <activity-->
        <!--            android:name=".LoginActivity"-->
        <!--            android:label="Preferences"></activity>-->
        <activity
            android:name=".activities.ViewUserProfileActivity"
            android:label="Preferences"></activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:label="Preferences"></activity>
        <activity
            android:name=".activities.FoodDonationActivity"
            android:label="Preferences"></activity>
        <activity
            android:name=".activities.HomeScreenActivity"
            android:label="Preferences"></activity>
        <activity
            android:name=".activities.ViewFoodDonation"
            android:label="Preferences" />
    </application>

</manifest>